# MongoDB Connection URI (Optional - fallback to local JSON file storage if not running)
MONGO_URI=mongodb://localhost:27017

# Gemini API Key (Optional - application runs in High-Fidelity Simulation Mode if missing)
GEMINI_API_KEY=your_gemini_api_key_here
